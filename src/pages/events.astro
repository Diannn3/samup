---
import BaseLayout from '../layouts/BaseLayout.astro';
import { flagshipEvents, getEventLocationLabel } from '../data/events';
import GlassCard from '../components/ui/GlassCard.astro';
import { Calendar, Users, ArrowRight, Sparkles, Trophy, MapPin } from 'lucide-react';
---

<BaseLayout
  title="Events & Programs | Society of Applied Mathematics of UPLB"
  description="Explore SAM-UP's flagship academic tournaments, peer tutorial caravans, research colloquiums, and athletic initiatives."
>
  {/* Page Header with Ambient Gradient Mesh */}
  <section class="py-20 sm:py-24 border-b border-[var(--border)] relative overflow-hidden">
    <div class="glow-blob w-[600px] h-[600px] bg-[radial-gradient(circle,rgba(240,204,78,0.12)_0%,transparent_70%)] top-0 left-[-10%]" aria-hidden="true" />
    <div class="glow-blob w-[500px] h-[500px] bg-[radial-gradient(circle,rgba(16,185,129,0.09)_0%,transparent_70%)] bottom-0 right-[-10%]" aria-hidden="true" />

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
      <div class="max-w-3xl space-y-4">
        <div class="coord-badge">
          <Sparkles class="w-3.5 h-3.5 text-[var(--primary)]" aria-hidden="true" />
          <span>OUTREACH, TOURNAMENTS & COLLOQUIUMS</span>
        </div>
        <h1 class="font-display font-black text-4xl sm:text-5xl lg:text-6xl text-[var(--foreground)] tracking-tight">
          Flagship Events & <span class="text-gold-gradient">Programs</span>
        </h1>
        <p class="text-base sm:text-lg text-[var(--muted-foreground)] leading-relaxed font-normal">
          From nationwide mathematical championships to grassroots peer tutoring caravans at the UPLB Math Building, SAM-UP initiatives champion academic excellence and service.
        </p>
      </div>
    </div>
  </section>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 space-y-16">
    {/* Events Grid — static Astro cards */}
    <div class="grid md:grid-cols-2 gap-8">
      {flagshipEvents.map((event) => (
        <GlassCard
          tag={event.category}
          title={event.title}
          subtitle={event.description}
          href={event.ctaLink || 'https://facebook.com/SAMUPLB'}
          showArrow={true}
        >
          <div class="space-y-3 pt-2">
            <div class="flex items-center gap-2 text-xs font-mono text-[var(--foreground)]/80">
              <Calendar class="w-3.5 h-3.5 text-[var(--primary)] shrink-0" aria-hidden="true" />
              <span>{event.schedule}</span>
            </div>

            <div class="flex items-center gap-2 text-xs font-mono text-[var(--foreground)]/80">
              <Users class="w-3.5 h-3.5 text-[var(--primary)] shrink-0" aria-hidden="true" />
              <span>Audience: {event.targetAudience}</span>
            </div>

            <div class="pt-3 border-t border-[var(--border-subtle)] flex items-center justify-between text-xs font-mono text-[var(--muted-foreground)]">
              <span class="flex items-center gap-1.5 text-[var(--muted-foreground)]">
                <MapPin class="w-3.5 h-3.5 text-[var(--primary)]" aria-hidden="true" />
                <span>{getEventLocationLabel(event)}</span>
              </span>
              <span class="text-[var(--primary)] font-semibold">{event.ctaText || 'Learn More'} →</span>
            </div>
          </div>
        </GlassCard>
      ))}
    </div>

    {/* Future Notice Frosted Banner */}
    <div class="frosted-glass-card p-8 sm:p-10 flex flex-col md:flex-row items-center justify-between gap-8 shadow-2xl">
      <div class="space-y-2 text-center md:text-left">
        <div class="flex items-center justify-center md:justify-start gap-2 text-xs font-mono text-[var(--primary)]">
          <Sparkles class="w-4 h-4" aria-hidden="true" />
          <span>STAY CONNECTED</span>
        </div>
        <h3 class="font-display font-bold text-2xl sm:text-3xl text-[var(--foreground)]">
          Looking for the upcoming semester event calendar?
        </h3>
        <p class="text-sm text-[var(--muted-foreground)] max-w-xl">
          Tournament registration dates, live bracket announcements, and peer tutorial caravan schedules are announced regularly on our official Facebook page.
        </p>
      </div>

      <a
        href="https://facebook.com/SAMUPLB"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center justify-center gap-2 px-6 py-3.5 rounded-xl bg-[var(--primary)] text-[var(--primary-foreground)] font-bold text-sm hover:bg-[var(--primary-hover)] transition-all shrink-0 shadow-md shadow-[var(--primary)]/20"
      >
        <span>Follow @SAMUPLB</span>
        <ArrowRight class="w-4 h-4" aria-hidden="true" />
      </a>
    </div>
  </div>
</BaseLayout>
