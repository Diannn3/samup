# Plan setup

Description: Generate and explain a safe setup plan without applying it.

1. Invoke the audit workflow.
2. Review and update `config/server.yaml` without changing the guild ID.
3. Load the `plan-server-setup` skill.
4. Generate and preview the plan.
5. Present all operations and wait for explicit user approval.
